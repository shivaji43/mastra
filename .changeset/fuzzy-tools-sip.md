---
'mastracode': minor
---

Added project-level MCP enable overrides and `/mcp inherit` for restoring global defaults. The MCP selector now shows both the project setting and global default, and hides global actions that would not change the setting.

```text
/mcp enable notion
/mcp inherit notion
```
