---
'@mastra/core': patch
---

Export `SlashCommandChannelHandler`, `SlashCommandChannelHandlerConfig`, and `SlashCommandEvent` from `@mastra/core/channels` so standalone slash-command handlers can be typed without reaching through `ChannelHandlers`.
