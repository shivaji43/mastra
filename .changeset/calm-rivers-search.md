---
'@mastra/parallel': minor
---

Added Parallel Search and Extract tools for Mastra agents through the new `@mastra/parallel` package.

```typescript
import { createParallelTools } from '@mastra/parallel'

const tools = createParallelTools()
```
